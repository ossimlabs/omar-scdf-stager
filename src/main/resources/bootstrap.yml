spring:
  application:
    name: omar-scdf-stager
  profiles:
      active: production
  cloud:
    config:
      enabled: true
      uri: http://scdf-config-server:8888/omar-config-server
    discovery:
      enabled: false